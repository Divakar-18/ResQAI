# Database schema

Canonical migrations live under [`../../supabase/migrations/`](../../supabase/migrations/). This directory holds a human-readable mirror plus seed helpers.

Apply with the Supabase CLI:

```bash
supabase db push
```
